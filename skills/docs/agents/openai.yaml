interface:
  display_name: "Docs"
  short_description: "Update repo docs and agent-facing guidance"
  default_prompt: "Use $docs to update AGENTS.md, README.md, docs/, specs, plans, or fix doc drift after code, skill, or infrastructure changes."
